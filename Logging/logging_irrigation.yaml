template:
  - trigger:
      - platform: event
        event_type: IRRIGATION_EVENT
    action:
      - service: logbook.log
        data:
          name: "Irrigation"
          message: "{{ trigger.event.data.message | default('event') }}"
          entity_id: "{{ trigger.event.data.entity_id | default('') }}"
